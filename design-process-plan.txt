DESIGN PROCESS PLAN
Portfolio site (evancmccaig/portfolio, theme: hugo-blog-awesome)
====================================================================

PURPOSE
--------
This is the shorthand format for specifying batches of content/design
changes in one shot instead of one prompt per edit. Write a list of
[Label] / [Content] pairs, hand it over, and it gets applied as one
pass: edited, built locally, verified, then committed and pushed
together.

This file is also the reference an AI agent (this one, or a different
one on a later session) reads FIRST before touching the site, so a
plan written today stays executable by whoever picks it up later.


FORMAT
------
Each instruction is one to three lines:

    [Label] Name of the element
    [Content] "The value to put there"

- Label   = which kind of element this is (see LEGEND below). A label
  is contextual, not a fixed 1:1 mapping — [Header] means something
  different next to "Title page" than it does next to "Projects
  Section". The Name line is what disambiguates it.
- Name    = which specific instance of that element.
- Content = the literal value — text in quotes, or a URL/file path
  for non-text elements (links, images).

Stack multiple instructions to batch unrelated changes together.
Order doesn't matter — they get grouped by which file they touch
before anything is edited.


LEGEND — label to site element
-------------------------------
| Label       | What it controls                             | Where it actually lives                              |
|-------------|-----------------------------------------------|--------------------------------------------------------|
| [Header]    | A top-level title — site identity or a new    | Site identity -> hugo.toml params.author.name/sitename |
|             | section's own heading                         | New section  -> content/<section>/_index.md title      |
| [Subheader] | The heading for one item inside a section     | content/<section>/<slug>.md title                       |
| [Sub-bio]   | A secondary descriptive line under a heading  | Homepage tagline -> hugo.toml params.author.description |
|             |                                                | Per-item bio -> body text in that item's .md file       |
| [Intro]     | Short line above the tagline (optional)       | hugo.toml -> params.author.intro                        |
| [Link]      | An embedded link/media element                | Raw HTML in the item's .md body (goldmark unsafe=true    |
|             |                                                | is enabled, so <iframe>/<div> embeds are allowed)        |
| [About]     | Body text of the About page                   | content/pages/about.md                                   |
| [Post]      | A blog post's title/body                      | content/posts/<slug>.md                                  |
| [Social]    | A social/contact link                         | hugo.toml -> [[params.socialIcons]]                       |
| [Avatar]    | Profile photo                                 | assets/avatar.jpg (file, not text)                        |
| [Menu]      | A nav bar entry                               | hugo.toml -> [[menu.main]]                                |

If a request uses a Name that doesn't map to anything that exists yet
on the site (e.g. "Projects Section" the first time it's mentioned),
that's a signal to BUILD the underlying structure first — new content
section, list/detail templates, menu entry — then apply the content
on top of it. See WORKFLOW step 2.


WORKFLOW — how to execute a batch (for any AI agent, not just this session)
------------------------------------------------------------------------------
1. PARSE. Read every [Label]/[Content] instruction, resolve each
   against the LEGEND, and write out the exact file + field each one
   will change. Flag anything genuinely ambiguous before touching
   files — but a Name that's simply new (see above) is not ambiguous,
   it's a build step.
2. BUILD STRUCTURE IF NEEDED. If an instruction targets a site element
   that doesn't exist yet (a new content section, a new nav item, a
   new embed pattern), create it before writing content into it:
     - New content section -> content/<section>/_index.md (list page)
       + content/<section>/<slug>.md (detail pages). Check whether the
       theme's existing layouts/list.html and layouts/single.html
       already render it acceptably before writing custom templates —
       this theme's generic templates cover most cases with zero new
       layout code.
     - New nav entry -> add a [[menu.main]] block in hugo.toml, with a
       `weight` placing it where it should sit in the nav order.
     - New embed/visual pattern -> prefer a small CSS addition in
       assets/sass/_custom.scss (this project-root file overrides the
       theme's own empty assets/sass/_custom.scss via Hugo's asset
       overlay — never edit the copy inside themes/hugo-blog-awesome/)
       over one-off inline styling, so the pattern is reusable next time.
3. APPLY CONTENT. Write the actual [Content] values into place.
4. BUILD LOCALLY. `hugo --gc --minify` from the repo root. Zero errors
   or warnings required before moving on.
5. VERIFY BY GREPPING OUTPUT. Don't trust "it compiled" — grep the
   actual built HTML under public/ for each changed value (title text,
   embed URL, CSS class actually present in the compiled stylesheet,
   etc.) so a silently-wrong template still gets caught.
6. CHECK VISUALS, DON'T JUST REASON ABOUT CSS. Grepping the HTML only
   proves the markup exists — it doesn't prove it's visible. Launch
   `hugo server` in the background, wait for it to actually respond
   (poll, don't sleep-and-hope), then screenshot the changed page:
     hugo server --port 1313 &
     (poll http://localhost:1313/ until 200)
     npx --yes playwright screenshot --wait-for-timeout=3000 \
       --full-page "http://localhost:1313/<path>/" <output>.png
   (chromium-cli is the preferred driver per the `run` skill; it
   wasn't installed on this machine, so this repo falls back to
   Playwright's one-shot `screenshot` command — `npx playwright
   install chromium` once if the browser binary is missing.) Actually
   look at the screenshot before declaring something rendered
   correctly. Stop the dev server afterward (kill whatever's listening
   on the port) so it doesn't linger.
7. RUN THE DOMAIN VERIFICATION CHECKLIST below.
8. COMMIT (one commit per batch, descriptive message) and PUSH — ask
   for confirmation before pushing unless told not to for that
   session.
9. RE-CHECK LIVE. After push, fetch the live URL and confirm the
   deploy picked up the change — a successful push is not the same as
   a successful deploy (Cloudflare can still fail the build; see the
   HUGO_VERSION incident logged below for what that looks like).


DOMAIN VERIFICATION CHECKLIST (run after every push)
-------------------------------------------------------
[ ] baseURL in hugo.toml matches the actual live URL
    (currently: https://portfolio-3i9.pages.dev/)
[ ] Cloudflare Pages "Deployments" tab shows the latest commit hash
    with a successful (not failed) build
[ ] HUGO_VERSION is pinned in Cloudflare Pages env vars (Production
    AND Preview) to a version that satisfies the theme's minimum
    (hugo-blog-awesome requires extended >= 0.160.0; this repo builds
    locally on 0.164.0)
[ ] Live page HTML actually contains the new content (fetch the live
    URL and grep for the changed string — don't trust "looks right in
    a screenshot", browser/CDN caching can show a stale page)
[ ] If the live page looks stale but the deployment succeeded: hard
    refresh / incognito test before concluding anything is broken —
    this is almost always cache, not a failed deploy
[ ] If a Retry/"force redeploy" of a previously-failed build still
    shows the identical error after fixing the underlying cause
    (e.g. an env var), don't trust it — Retry can re-run against the
    config snapshotted at original trigger time. Push a new commit
    (an empty one is fine) to force a genuinely fresh build.


BATCH LOG
----------
2026-08-12 — Malformed `date` front matter broke the whole build
    content/projects/hand-to-hand-wizardry.md's `date` field got
    hand-edited to a non-date string ('Hand to Hand Wizardry'). Hugo
    treats an unparsable date as a fatal error, not a per-page
    warning — the ENTIRE site fails to build, not just that page. This
    was never pushed (caught locally), but it's why "the embed isn't
    visible" can have nothing to do with the embed itself — always
    run a clean `hugo --gc --minify` and read the actual error before
    assuming a rendering/CSS bug. Confirmed via WORKFLOW step 6
    (headless-browser screenshot) that the embed renders correctly
    once the date was fixed — no CSS bug existed.

2026-08-12 — Theme swap (Quint -> hugo-blog-awesome) + HUGO_VERSION fix
    Swapped theme submodule, rebuilt hugo.toml for the new theme,
    removed Quint-specific overrides. First deploy failed:
    `.Site.Language.Locale` template error — Cloudflare's default
    build image predates that Hugo field. Root cause: HUGO_VERSION was
    never pinned in Cloudflare Pages env vars. Fixed by setting
    HUGO_VERSION=0.164.0 for both Production and Preview, then a fresh
    push (Retry alone did not pick up the new setting).

2026-08-12 — Web process batch: hero content + Projects section
    [Header] Title page / [Content] "Evan McCaig"
      -> already matched hugo.toml params.author.name, no change.
    [Sub-bio] Tagline / [Content] "Game Designer | C# | Unity | ..."
      -> already matched params.author.description, no change.
    [Header] Projects Section / [Content] "Projects"
      -> new section, didn't exist yet. Built:
         content/projects/_index.md (list page, reuses theme's
         generic layouts/list.html), hugo.toml [[menu.main]] entry
         (weight 20, between Home and Posts).
    [Subheader] Name of first project / [Content] "hand to hand wizardry"
      -> content/projects/hand-to-hand-wizardry.md, title
         "Hand-to-Hand Wizardry" (reuses theme's generic
         layouts/single.html).
    [Link] Embed YouTube, 1920x1080 / [Content] youtube.com/watch?v=C3mWOifDKh4
      -> responsive 16:9 <iframe> embed in the project's .md body,
         wrapped in a .video-embed div. New CSS added to
         assets/sass/_custom.scss (aspect-ratio box, scales to the
         container, caps at 1920px so it never upscales past source
         resolution — a literal fixed 1920x1080 element would overflow
         small screens, so this achieves the same visual ceiling
         responsively instead).
    [Sub-bio] Game bio below the embed / [Content] "Hand-to-Hand
    Wizardry delivers a first-person, dark fantasy retro-shooter..."
      -> body paragraph directly under the embed in the same file.
    "Level design assets:" (no content yet) -> left as a
      "## Level Design Assets / _Coming soon._" placeholder heading
      on the project page.
    Verified: clean build, grep-confirmed embed URL, CSS class in
    compiled stylesheet, nav link, and bio text all present in
    public/ output before commit.


2026-08-12 — Portfolio-style Projects section (drop date, bold title)
    Request: revise Projects to read as a portfolio, not a blog —
    remove the date, make the project title the main bolded element,
    focus on readability. Re-submitted the same [Header]/[Subheader]/
    [Link]/[Sub-bio] instructions as the earlier batch (content
    unchanged, already in place) plus this new styling ask.
    Generic theme templates (layouts/single.html, layouts/list.html)
    are shared by BOTH posts and projects, so a global CSS hide-date
    rule would have wrongly hidden dates on blog posts too. Built
    section-specific overrides instead — Hugo resolves
    layouts/<section>/single.html and layouts/<section>/list.html
    ahead of the generic ones, so only content/projects/* is affected:
        layouts/projects/single.html   - drops the .post-meta/date
          block entirely, adds a .project-title class to the <h1>
        layouts/projects/list.html     - drops the theme's
          GroupByDate year-heading loop, ranges pages directly
        layouts/_partials/projectCard.html - new partial (bold title,
          optional description teaser below it), replacing postCard's
          flex date+title row layout which doesn't apply without a date
        assets/sass/_custom.scss       - .project-title (bold),
          .project-list/.project-item/.project-item-title/
          -description (block-stacked cards, bold title, muted teaser)
    Verified: clean build, grep-confirmed .post-meta absent and
    .project-title/.project-item-title present in output, theme's own
    generic templates untouched (blog posts keep their date when
    added). Visual check (workflow step 6): screenshotted both
    /projects/ and /projects/hand-to-hand-wizardry/ — title reads as
    the clear bold primary element on both, no date, video/bio intact.


2026-08-12 — Avatar photo + homepage nav cards (replace "Recent Posts")
    Avatar: assets/avatar.jpg appeared in the working tree as a 17MB,
    6240x4160 camera original (Canon EOS 6D Mark II). Flagged the size
    before committing; downscaled to 800px longest edge + stripped
    EXIF via Pillow (17MB -> 41KB) before committing, since the theme
    only ever displays it downscaled to ~210px. hugo.toml already
    pointed params.author.avatar at "avatar.jpg" from the original
    theme setup, so no config change was needed once the file existed.
    Homepage nav: replaced the theme's "Recent Posts" feed (empty
    anyway - no blog posts exist) with two link cards to Projects and
    About. New layouts/home.html (project-root override of the
    theme's home.html) + .home-links/.home-link* in
    assets/sass/_custom.scss. While adding dark-mode colors for the
    new cards, found and fixed a pre-existing gap: .project-item-title
    link color and .project-item-description from the previous batch
    were also missing dark-mode overrides (the theme's own dark-mode
    mixin only knows about theme-native classes, not project-root
    ones) - both fixed together via one @mixin custom-dark-mode,
    triggered the same way the theme triggers its own (prefers-color-
    scheme media query guarded by html:not(.light), plus html.dark).
    Verified: clean build, grep-confirmed Recent Posts absent and both
    link cards present. Visual check in BOTH light and dark
    (prefers-color-scheme) via Playwright, colorScheme: 'dark' context
    option - avatar renders cropped correctly, cards legible and
    correctly colored in both modes.


2026-08-12 — Added Level Design + Programming sections to Hand-to-Hand
Wizardry; investigated "embed not working" report
    Investigated a report that the YouTube embed wasn't rendering.
    Checked both local build and the live deployed HTML directly
    (curl, not just eyeballing) - the <iframe>/video-embed markup was
    identical and correct in both. Playwright screenshot showed the
    embed rendering fine with a real YouTube thumbnail. Couldn't
    reproduce - most likely explanation is stale CDN/browser cache
    (same class of issue as the earlier domain-verification incident),
    not a code bug. No fix applied since nothing was actually broken;
    flagged to the user to hard-refresh/incognito-check post-deploy.
    Added two new body sections ("Level Design", "Programming") styled
    to match the page's own title exactly. Used Hugo's markdown
    heading-attribute syntax (## Heading {.section-heading}) rather
    than raw HTML for the headings specifically so they still get
    picked up by the Table of Contents - raw HTML headings (like the
    video-embed div) bypass Goldmark's heading/TOC extraction entirely.
    New .section-heading class in assets/sass/_custom.scss - set
    explicitly (font-size 2em, weight 800) rather than reusing
    .header-title, since that theme rule is scoped to ".header
    .header-title" and doesn't apply to a heading placed in body
    content. Both new sections use clearly-labeled placeholder text
    (no real copy was given for either) rather than fabricated-sounding
    filler, matching the existing "Level Design Assets / _Coming
    soon._" convention already on this page.
    Verified: clean build, grep-confirmed section-heading class
    attached and both new headings appear in the compiled
    TableOfContents. Visual check via Playwright screenshot - both
    sections render with the same bold weight as the page title, embed
    still intact, existing content unaffected.


2026-08-12 — Remove Table of Contents from all project pages
    Removed the {{ partial "toc.html" . }} call from
    layouts/projects/single.html (the section-specific template)
    rather than adding a per-file `toc = false` front matter flag —
    this way it's off for every current and future project page by
    default, not just the one being edited at the time. Blog posts
    and other sections are unaffected (they use the theme's generic
    single.html, untouched). Verified: clean build, grep-confirmed
    class="toc" absent from the built page; visual check via
    Playwright screenshot.


2026-08-12 — Second project (The Salad Bowl), reusing the established
template with a still image instead of a video
    Mirrored Hand-to-Hand Wizardry's structure exactly: title, lead
    media, bio paragraph, ## Level Design {.section-heading}, ##
    Programming {.section-heading} - all placeholder text since no
    real content was given for this project yet.
    Lead media is a PNG, not a YouTube embed. Rather than duplicate
    the 16:9 box CSS, factored the shared box properties out of
    .video-embed into a @mixin media-embed-box, then added a sibling
    .image-embed class (img + object-fit: cover, so a source image
    that isn't exactly 16:9 crops instead of distorting or
    letterboxing) - .video-embed itself is unchanged, so Hand-to-Hand
    Wizardry's embed is unaffected.
    Image file doesn't exist yet. Referenced it at
    static/images/projects/the-salad-bowl.png (Hugo copies static/ to
    the site root as-is - no page-bundle restructuring needed). Chose
    static/ over assets/ (like avatar.jpg uses) because there's no
    resizing/processing need here - the source is expected to already
    be roughly 16:9, and object-fit: cover absorbs minor mismatches.
    Verified: clean build, grep-confirmed image-embed class and img
    src path in output. Visual check via Playwright screenshot -
    confirmed the box holds its correct 16:9 shape via CSS
    aspect-ratio even with the image file missing (broken-image icon
    + alt text inside a correctly-sized box), proving the structure
    works independently of whether the file exists yet.


2026-08-12 — The Salad Bowl's real lead image
    User supplied the actual image (JPG, 1521x954, from
    Desktop\Wizardry\TheSaladBowlThumbnail.jpg) - copied to
    static/images/projects/the-salad-bowl.jpg (note: .jpg, not the
    .png placeholder path used when the section was first scaffolded;
    updated the .md reference and CONTENT-GUIDE.md's wording from
    PNG-specific to <ext>-generic accordingly).
    Source aspect (1.594:1) doesn't exactly match the 16:9 box
    (1.778:1) - checked the math before shipping (object-fit: cover
    scales to the constraining dimension and crops the other): here
    width matches the box exactly and height overflows by ~10%,
    cropped symmetrically top/bottom. Confirmed via visual check that
    this crops only ceiling/floor margin, not the framed subject (the
    altar/arena center or the cleaver on the right edge).
    Verified: clean build, grep-confirmed the new path in output and
    file present in public/images/projects/. Visual check via
    Playwright screenshot against the real image.


2026-08-12 — Investigated "missing subheadings" report — false alarm
    Report: both project titles look right, but the Level Design /
    Programming subheadings and the Salad Bowl thumbnail were said to
    be missing. Checked before changing anything, per WORKFLOW step 6:
    - Source .md files for both projects already had both
      {.section-heading} sections and correct content.
    - Live site (curl'd directly, not just eyeballed) already
      rendered both headings correctly on Hand-to-Hand Wizardry and
      The Salad Bowl.
    - Clean local rebuild + full-page Playwright screenshots of both
      pages confirmed everything present and correctly styled.
    Nothing was actually broken - this is the same class of issue as
    the earlier "embed not visible" report: a stale cached view on the
    user's end, not a deploy or code problem. No content or template
    change was needed or made.
    Found and removed one incidental thing: a duplicate copy of
    TheSaladBowlThumbnail.jpg had been dropped in the repo root
    (identical file, confirmed by hash, to the one already correctly
    placed at static/images/projects/the-salad-bowl.jpg) - deleted it
    since it was untracked and redundant; nothing referenced it.


2026-08-12 — Fixed 4 broken Level Design images on Hand-to-Hand Wizardry
    User added 4 <div class="image-embed"><img ...></div> blocks
    (same pattern as The Salad Bowl) but none rendered. Root cause
    wasn't the embed method - it was genuinely broken file references:
    the .md pointed at Library2.jpg / Bridge.jpg / Library.jpg /
    Bridge2.jpg, but the actual committed files were
    Library2.jpeg / Bridge1.jpeg / Library.jpeg / Brdige2.jpeg (note
    the extension mismatch and the "Brdige2"/"Bridge1" name
    mismatches). Fixed the four src paths to match the real filenames
    exactly rather than renaming the image files, and set alt text to
    ld1/ld2/ld3/ld4 per request (was all "The Salad Bowl", copy-paste
    leftover). Verified: clean build, confirmed all 4 files exist at
    their referenced paths in public/, visual check via full-page
    screenshot showed all 4 actual level design screenshots rendering
    correctly in their 16:9 boxes.


2026-08-12 — Center-align project page titles
    Reused the theme's own existing .header-title.center modifier
    (already used on the Projects list heading) rather than writing
    new CSS - added "center" to the h1 class list in
    layouts/projects/single.html. Applies to all project pages via
    the shared section template, not per-file. Verified: clean build,
    grep-confirmed class on both Hand-to-Hand Wizardry and The Salad
    Bowl; visual check via screenshot of both page tops.


2026-08-12 — New .section-image / .section-images classes for smaller
inline images (vs. full-width .image-embed)
    Request: .image-embed works well for a project's header/lead
    media but is wrong for smaller supporting images inside a
    subheader section - it forces a fixed 16:9 box with absolute-
    positioned, object-fit: cover content, meant for one dominant
    piece of media, not an inline screenshot at natural aspect ratio.
    Added .section-image (block, natural aspect ratio, max-width
    500px, centered, no wrapping div needed - just add the class
    straight to an <img>) and .section-images (flex row wrapper for
    two-plus side by side).
    Caught and fixed a real bug before shipping: the first pass had
    .section-images .section-image inheriting max-width: 500px from
    the base rule, so two side-by-side images (500+500+gap) didn't
    fit in a normal content column and silently wrapped to stacked
    instead of sitting side by side - looked identical to just using
    two plain .section-image tags with no visual indication anything
    was wrong. Found this via an ISOLATED test page (compiled CSS +
    a real image file, no Hugo content touched) rather than editing
    real project pages to test with - kept the verification fully
    separate from content changes. Fixed with max-width: calc(50% -
    0.5rem) scoped to the .section-images context specifically.
    No real project content was changed - this batch only adds the
    capability, per request. Verified via the isolated test page
    (single image + side-by-side pair, both screenshotted) rather
    than the live site, since nothing user-facing changed yet.


2026-08-12 — Swapped .image-embed for .section-image on Hand-to-Hand
Wizardry and The Salad Bowl
    Request: replace every .image-embed usage in both files with the
    new .section-image class, keeping the same jpeg/jpg files. Note
    this included The Salad Bowl's HEADER image too (not just its
    subsection images) - "all" was read literally, so the header
    image lost its full-width 16:9 hero treatment and now displays at
    the smaller, natural-aspect-ratio .section-image size like every
    other image on that page. Flagged this to the user since it's a
    real visual hierarchy change, not just a mechanical swap.
    Hand-to-Hand Wizardry's .video-embed (the YouTube iframe) was left
    untouched - only .image-embed instances were in scope.
    While rebuilding to verify, hit an unrelated but build-breaking
    problem: a third project file (last-night-at-deer-lake.md, not
    part of this request) had its closing +++ misplaced after a
    <div class="video-embed"> block instead of right after the front
    matter fields, so TOML tried to parse the HTML as config and
    failed the ENTIRE site build. Fixed the delimiter placement only
    (moved +++ to the correct spot) since that's a structural bug
    blocking everyone, not a content decision - did not touch that
    file's actual content/copy, which is that project's own separate
    unfinished work.
    Verified: clean build, grep-confirmed zero image-embed occurrences
    remain in either target file, .video-embed still present in HTH,
    all original file paths unchanged. Visual check via full-page
    screenshots of both pages.


ROADMAP — content not yet ready to implement (no assets exist yet)
-----------------------------------------------------------------------
These are from the original "Portfolio Content Plan" note but aren't
[Label]/[Content] instructions — they're prerequisites that have to
happen outside this repo first (recording, editing, writing) before
there's anything to put on the page.

- Project page (generic template: footage/screenshots/trailer, high
  concept, contributions, design process) — est. 45 min once assets exist
- Level Design Documentation (1-minute YouTube video: combat
  calculator, modular kit building, timelapse layouts, narrated design
  reasoning, edited in Premiere Pro) — est. 4 hours
- Git page for the Hand-to-Hand Wizardry gameplay event bus (readable
  script, demo folder with example scripts/prefabs/docs, screenshots
  formatted onto the GitHub repo page, then linked from the portfolio) — est. 2 hours


OPEN ITEMS NOT YET COVERED BY THE LEGEND
-------------------------------------------
- Custom domain (currently on the free *.pages.dev subdomain by choice)
- Avatar photo (assets/avatar.jpg not yet added)
- Real social links beyond GitHub + RSS
- Hand-to-Hand Wizardry's "Level Design Assets" section on its project
  page (screenshots/timelapse from the roadmap item above)
